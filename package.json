{
  "name": "install-node",
  "version": "1.0.0",
  "description": "Install node.js",
  "author": "Mapbox",
  "license": "ISC",
  "private": true,
  "scripts": {
    "test": "for file in ./test/test_*.sh; do $file; done"
  },
  "bin": {
    "install_node": "./bin/install_node"
  }
}
